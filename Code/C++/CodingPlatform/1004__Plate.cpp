#include <bits/stdc++.h>
using namespace std;

int main(){
	int x,y; cin >> x >> y;
	set<pair<int,int> >s;
	for(int i=0;i<y;i++){
		cin >> s.first >> s.second;
	}
	char ch;
	while(true){
		cin >> ch;
		if(ch=='X') return 0;
		if(ch=='E') 
	}
}
