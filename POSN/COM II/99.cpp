#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    int n,k;
    cin >> n >> k;
    int x[n];
    for(int i=0;i<n;i++){
        cin >> x[i];
    }
    int k = 0;
    int l = k+1;
    int r = n-1;
    while(l<r){
        
    }
    return 0;   
}